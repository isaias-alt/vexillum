# 2026-09-22 - Pruebas de la v2 vía el commander real (no CLI directo) + bug de no-mistakes encontrado en vivo

Continuación directa de `20260921-tanda-pruebas-vivo-v2.md` (esa fue corrida por el asistente ejecutando comandos `vexillum` directamente; esta la corrió el general hablándole al commander en lenguaje natural, para validar que `AGENTS.md` guía bien el comportamiento real - el asistente solo leyó las sesiones vía `herdr pane read` para revisar cada paso).

## Resuelto y en pie

- **Metodología**: se armó una secuencia de 5 prompts (+ 1 extra agregado a pedido del general) para pasarle al commander, cada uno pensado para forzar el uso de una pieza distinta de la v2 a través de comportamiento natural, no comandos crudos. El asistente revisó cada paso leyendo la sesión real del commander con `herdr pane read <pane_id>` (no `herdr agent read`, que requiere un agente nombrado - el commander mismo no tiene ese tipo de nombre).

- **Paso 1 (doctor + B.1 quota-axi)**: perfecto. Corrió `doctor` sin pedírselo dos veces, cargó el skill de `quota-axi` de verdad (`Skill(quota-axi)` → "Successfully loaded skill"), reportó datos reales de cuota (43%, spend priority -1.04, agotamiento proyectado). **Bonus no pedido**: le llegó un Stop hook feedback sobre dos misiones viejas de la tanda de pruebas anterior del asistente, las investigó solo y confirmó que no necesitaban acción - confirma que el async Stop hook sigue funcionando encadenado con trabajo de otra sesión.

- **Paso 2 (B.2 lavish-axi)**: confirmado por el general con detalle ("se abrió, pude anotar, devolví, mejoró el plan, y luego lo hizo") - el ciclo completo de revisión interactiva funcionó, y el plan final incluyó una función extra (`suggest_email_correction`) que no se había pedido explícitamente, evidencia de que la revisión realmente aportó algo. **Nota operativa encontrada**: la sesión de Claude Code de ese pane se cerró y reabrió entre pasos (`cc`/"Resume this session with..."), y el buffer de la TUI no sobrevive a ese ciclo (usa alternate screen, no scrollback normal) - el asistente no pudo leer ese paso hasta que el general la resumió de nuevo. Land posterior pisó el caso ya documentado en `AGENTS.md` (scaffold de `vexillum upgrade` sin commitear) y lo resolvió exactamente como dice la guía.

- **Paso 3 (B.3 chrome-devtools-axi, navegar + extraer)**: dos misiones reales, ambas verificadas - Hacker News (título real "MiMo v2.6") y el blog de Engineering de Anthropic (título real "How we contain Claude across products"), cada una comiteada y aterrizada (la segunda con una rebase correctamente delegada al soldier, como documenta `AGENTS.md`).

- **Prueba extra (B.3, click + fill + submit)**: agregada a pedido del general para ejercitar la otra mitad de las capacidades documentadas de chrome-devtools-axi (navegar/clickear/llenar/extraer - solo se había probado navegar/extraer). Mission completó un formulario real en `httpbin.org/forms/post` y lo envió. **Verificado leyendo el archivo comiteado directamente** (no solo el resumen del commander): el JSON de respuesta de httpbin es real, con los datos exactos enviados. **Corrección a un hallazgo de la sesión anterior**: el `User-Agent` en la respuesta es `HeadlessChrome/153.0.0.0` - no reusa el Chrome visible del general como se había hipotetizado antes, lanza un Chrome headless propio. Explica por qué nunca aparece una ventana de browser en ningún caso de uso de chrome-devtools-axi.

- **Paso 4 (A.2 redispatch, vía commander)**: resultado de manual perfecto. Dispatchó, el general cerró el pane, sentinel marcó `interrupted`, el commander investigó el camp directamente (encontró un archivo sin commitear, sin tests), y **citó explícitamente `AGENTS.md` para justificar por qué no iba a redespachar por su cuenta** ("descarta ese camp por completo... te pregunto cómo seguir"). Recién redespachó tras la aprobación del general, con el mismo task id, y el segundo intento terminó, comiteó y aterrizó limpio.

- **Paso 5 (B.4 no-mistakes / vexillum ship) - encontró un bug real en la herramienta externa, no en vexillum**:
  - Primer intento de `ship` fue sobre una tarea ya aterrizada y liberada (el general había pedido aterrizar la mission de `stack_queue.py` antes) - el commander explicó correctamente que no había camp para shippear y preguntó cómo seguir. Se dispatchó una mission nueva (`priority_queue.py`, cola de prioridad) específicamente para probar el ship.
  - **Antes de shippear**, el commander notó una discrepancia en el conteo de tests reportado por el soldier (73 vs 100 esperados) y la investigó por su cuenta antes de proceder (era un problema de scope entre `unittest discover` y `pytest`, no un problema real - confirmó 114 tests pasando con pytest). Buena disciplina antes de una acción irreversible.
  - `vexillum ship` corrió el auto-gate, que falló porque el proyecto no tenía remote `origin` - mensaje real y claro de `no-mistakes init`, igual que en la prueba anterior del asistente. **A diferencia de esa prueba**, acá el general decidió resolverlo de verdad: le pidió al commander crear un repo real en GitHub. El commander preguntó la visibilidad (el general eligió privado), creó `github.com/isaias-alt/vexillum-prueba` (privado, cuenta `isaias-alt`, vía `gh` ya logueado), pusheó `main`, y reintentó `ship` - esta vez el auto-gate funcionó y el pipeline arrancó de verdad.
  - **Bug real encontrado**: el pipeline avanzó por `intent` y `rebase` sin problema, pero el paso **`review` se colgó indefinidamente** - el log mostraba el veredicto completo del reviewer ya escrito, pero el step nunca transicionó a `completed`. El commander investigó en profundidad antes de escalar: confirmó que el proceso seguía vivo (PID activo), que tenía una conexión TCP `ESTABLISHED` hacia la API (no colgado en un deadlock local), y que el log ya tenía contenido final - concluyó que se traba terminando la respuesta estructurada (JSON schema) o el cierre del turno, no por pérdida de conexión.
  - **Se reprodujo dos veces**, en el mismo punto exacto, con reintentos distintos (`vexillum ship` la primera vez, `no-mistakes rerun` la segunda porque `ship` no genera un run nuevo si el commit no cambió - el commander lo descubrió y resolvió solo). El commander concluyó correctamente que es "un problema sistémico de no-mistakes en este repo, no mala suerte" antes de que el general lo confirmara.
  - El general lo confirmó y pidió abortar sin más reintentos automáticos; el commander respetó la respuesta custom ("Solo abortar", no una de las opciones que había ofrecido) sin insistir. Se resolvió con `vexillum land` local como respaldo, exactamente la alternativa que ya documenta `AGENTS.md` para cuando el gate no está disponible.
  - **Versión de `no-mistakes` en la que se reprodujo**: `v1.79.0 (fc540ac)`, 2026-09-19. Runs de no-mistakes confirmados en estado `cancelled` (limpio, sin quedar colgados) - `no-mistakes runs` los lista bien; no quedó ningún proceso zombie del run en sí (el daemon persistente de `no-mistakes` sigue corriendo, eso es esperado, no un leak).

- **Efecto externo real de esta sesión, autorizado explícitamente por el general**: se creó un repositorio real y privado en GitHub, `github.com/isaias-alt/vexillum-prueba`, con `main` pusheado. No es un efecto secundario oculto - fue una decisión explícita del general para poder probar el pipeline completo de `no-mistakes`.

- **Complicación operativa de esta sesión (ya resuelta)**: `no-mistakes` no quedó en el PATH tras instalarlo (el script de instalación pide `sudo` interactivo para symlinkear a `/usr/local/bin`, que ni el asistente ni una sesión no interactiva pueden completar). El `~/.zshrc` del general tampoco es editable a mano - es un symlink gestionado por Nix home-manager, de solo lectura. El general lo resolvió correctamente agregando el PATH en su `home.nix` (la vía declarativa correcta para su setup), no parcheando `.zshrc`.

## Pendiente para la próxima

- **Investigar el bug de `no-mistakes` review colgado** (no es un bug de vexillum): confirmar si es conocido/reportado upstream, si hay una versión más nueva que lo arregle (`no-mistakes update`), o reportarlo en `github.com/kunchenguid/no-mistakes/issues` si no está ya. Hasta que se resuelva, `vexillum ship` en este repo particular no puede completar un pipeline real - queda funcionalmente probado hasta donde vexillum controla (push determinista, auto-gate, manejo de fallos), pero sin un PR real abierto todavía.
- Reintentar `vexillum ship` una vez que se sepa si el bug de `no-mistakes` tiene arreglo, ahora que `vexillum-prueba` ya tiene un remote real (`github.com/isaias-alt/vexillum-prueba`) y no hace falta resolver eso de nuevo.
- La distribución brew/curl del binario `vexillum` con todos los cambios de la v2 sigue pendiente, después de comitear todo (`docs/testing-distribution.md`).

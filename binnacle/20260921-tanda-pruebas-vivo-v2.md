# 2026-09-21/22 - Tanda de pruebas en vivo de la v2 (A.2, B.1-B.4)

Corrida por el asistente en `~/github/isaias-alt/tmp/vexillum-prueba`, workspace real de herdr "PRUEBA VEXILLUM" (`wW`), a pedido explícito del general.

## Resuelto y en pie

- **Sentinel viejo detectado y reiniciado antes de empezar** - había uno corriendo con el binario compilado en el repo (`~/github/isaias-alt/vexillum/vexillum sentinel`, pid 32935), de una sesión anterior. Se mató y se dejó que `dispatch` levantara uno fresco con el binario recién buildeado - mismo error que ya había mordido en binnacles anteriores, evitado esta vez a propósito.

- **B.1/B.2/B.3 (skills instalados)**: se instalaron los tres AXIs de verdad con `npx skills add`.
  - **Hallazgo real**: instalar `-g` (global) en esta máquina rompe la detección, porque `~/.claude/skills` es en sí mismo un symlink manejado por dotfiles (`~/github/isaias-alt/dotfiles/home/.claude/skills`) - el symlink relativo que crea `npx skills add -g` (`../../.agents/skills/<nombre>`) se resuelve relativo a esa ubicación real, no a `~/.claude/skills/` como aparenta, y termina apuntando a un lugar que no existe. **Decisión del general**: instalar siempre project-local (sin `-g`) en vez de global. Los tres (`quota-axi`, `lavish`, `chrome-devtools-axi`) instalados así en `vexillum-prueba` y confirmados por `vexillum doctor`: `[installed] quota-axi`, `[installed] lavish`, `[installed] chrome-devtools-axi`. B.1 y B.2 quedan verificados en vivo end-to-end (instalación real + detección real).

- **A.2 (redispatch) verificado de punta a punta contra herdr real**:
  - Encontrado en el camino: un `sleep` (foreground o backgrounded a mano) no sirve para mantener una mission "trabajando" el tiempo suficiente para alcanzar a cerrar el pane - Claude Code v2.1.267 backgroundea sleeps largos por su cuenta ("I'll be notified when..."), y el agent_status pasa a idle/done igual. Lo que sí funcionó: una tarea real multi-paso (escribir un módulo + tests + correr pytest) que naturalmente supera los ~15s del quick-settle probe.
  - Dispatch real (`123229156d3923d8`), `herdr tab close` a mano, sentinel marcó `interrupted` a los **10.04s exactos** de `agent_not_found_since` (coincide con `notFoundConfirmWindow=10s`, igual que en la ronda de pruebas de la sesión anterior).
  - `vexillum redispatch` real: mismo task id, `redispatches: 1`, `agent_not_found_since` limpio, camp descartado de verdad (el `calculator.py` sin commitear del intento muerto desapareció - confirmado comparando el commit final, que es nuevo, no el residuo viejo), soldier nuevo terminó y comiteó solo. **No se volvió a marcar interrupted** - el bug que se cuidó explícitamente al implementar A.2 no se reprodujo.
  - Ciclo cerrado con `land` + `release` reales (con una divergencia real en el medio - se había comiteado la instalación de los skills en el proyecto real mientras tanto - resuelta con un rebase manual, exactamente el escenario que ya documenta `productAgentsMD`).

- **B.3 (chrome-devtools-axi), hallazgo real que corrige la documentación investigada**:
  - Al pedirle a un soldier real que usara `chrome-devtools-axi` para navegar a `https://example.com`, el soldier se bloqueó solo: chequeó el paquete en npm antes de correrlo y encontró que se había publicado hace 11hs, de un mantenedor sin historial aparente - pidió confirmación explícita antes de ejecutar `npx` (código arbitrario). Se investigó más a fondo (`npm view chrome-devtools-axi time --json`): el paquete tiene **36 versiones desde 2026-03-27** (~6 meses), publicado vía **GitHub Actions con OIDC** (trusted publishing, no una cuenta personal) - la lectura inicial del soldier era conservadora pero incompleta, no incorrecta como principio. Confirmado con el general, se autorizó correr el paquete.
  - **El paquete corrió bien** (escribió `browser_result.txt` con el título de la página), pero **nunca apareció `bridge.pid`** - ni durante la ejecución ni después. Solo se vieron `snapshot-generation` (persiste) y un `selected-page-id` transitorio. Tampoco quedó ningún proceso `chrome-devtools-axi`/bridge corriendo en `ps aux` una vez terminado - reusó el Chrome ya abierto del usuario vía CDP, sin dejar nada vivo.
  - **Conclusión**: la documentación pública describe un modo "bridge persistente" que aparentemente no se activa en el uso default de una sola invocación (probablemente reservado a `chrome-devtools-axi server` o modo compartido explícito). `stopOrphanBrowser` (que busca `bridge.pid`) es seguro tal cual está - nunca invoca nada sin evidencia - pero es probable que nunca encuentre nada que limpiar en el caso de uso más común. No se tocó código a partir de este hallazgo en esta sesión; queda anotado como pendiente de revisión (ver abajo).

- **B.4 (no-mistakes) verificado parcialmente en vivo**:
  - Instalado el binario real (`curl | sh`) - el paso de symlink a `/usr/local/bin` pidió `sudo` interactivo, que el asistente no tiene forma de completar (sin terminal para la contraseña) - se usó el binario ya descargado en `~/.no-mistakes/bin/no-mistakes` directamente vía PATH, sin escalar privilegios. **Nota para el general**: falta correr `sudo ln -sf ~/.no-mistakes/bin/no-mistakes /usr/local/bin/no-mistakes` (o repetir el instalador con terminal interactiva) para tenerlo disponible sin anteponer el PATH a mano.
  - `vexillum doctor` detectó el binario real y reportó correctamente "no gateado".
  - `vexillum ship` sobre una mission `done` real: corrió el auto-gate (`no-mistakes init`) como está diseñado, que **falló limpio** porque `vexillum-prueba` no tiene remote `origin` real - `no-mistakes` mismo lo explica ("no-mistakes pushes your branch and opens a pull request, so it needs a remote to push to"). `ship` reportó el fallo real de la herramienta y no intentó ningún push. Comportamiento exactamente como se diseñó (`TestRunShip_AutoGateFailureIsReported`), confirmado con la herramienta real, no un stub.
  - **No se probó el pipeline completo ni la apertura de un PR real** - requiere un remote real de GitHub, que es un efecto externo visible (crear un repo, pushear, que se abra un PR de verdad) que no se hizo sin permiso explícito del general.

## Pendiente para la próxima

- **B.3**: revisar si `chrome-devtools-axi` tiene un modo que sí deja un bridge persistente (server/shared), y si corresponde, decidir si `stopOrphanBrowser` necesita ajustarse (otro archivo a chequear, u otra lógica) o si directamente el escenario de "browser huérfano" del PRD no aplica al modo de uso por default y se documenta como tal.
- **B.4**: para probar el pipeline completo (review/test/lint reales + apertura de PR), hace falta un repo real de GitHub como remote de `vexillum-prueba` (o un repo de prueba dedicado) - pedir permiso explícito antes de crear uno, dado que es un efecto externo real y visible.
- Symlinkear `no-mistakes` a `/usr/local/bin` (necesita `sudo`, el general lo corre a mano).
- Distribución brew/curl del binario `vexillum` con todos los cambios de esta sesión - sigue después de comitear todo, según `docs/testing-distribution.md`.

## Limpieza

Todos los artefactos de esta ronda quedaron limpios al terminar: los 4 camps usados hoy (slots 5, 6, 7, 8) liberados o vueltos a estado limpio, ramas de prueba borradas, tabs de herdr cerrados (`PRUEBA VEXILLUM` volvió a 1 tab, su estado original), tasks/wakes de prueba eliminados. Los slots 1-4 del pool (de sesiones anteriores) no se tocaron - no había contexto suficiente sobre su contenido para tocarlos con seguridad. `vexillum-prueba` quedó con 3 commits nuevos reales (instalación de skills, módulo calculator, ship_test.txt) sobre `main`, todo aterrizado limpio.
